import React, { useEffect } from "react";

export default function RadioBtn(selected) {
  return (
    <div
      selected={allVideosData[i]?.selected}
      onClick={() => handleSliderChange(allVideosData[i], i)}
    ></div>
  );
}
